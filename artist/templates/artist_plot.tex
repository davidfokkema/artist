% \usepackage{tikz}
% \usetikzlibrary{arrows}
% \usepackage{pgfplots}
% \pgfplotsset{compat=1.3}

\begin{tikzpicture}[
        every pin/.style={inner sep=0mm},
        every pin edge/.style={<-, >=stealth', shorten <=2pt},
        pin distance=2.5ex,
    ]
    \begin{{ '{' }}{{ axis }}}[
            width={{ width }},
            %
            xlabel={ {{ xlabel }} },
            ylabel={ {{ ylabel }} },
            %
            xmin={ {{ limits.xmin }} },
            xmax={ {{ limits.xmax }} },
            ymin={ {{ limits.ymin }} },
            ymax={ {{ limits.ymax }} },
            %
            xtick={ {{ ticks.x | join(', ') }} },
            ytick={ {{ ticks.y | join(', ') }} },
            %
            tick align=outside,
            max space between ticks=40,
            every tick/.style={},
        ]

        {% for series in series_list %}
            \addplot[{{ series.options }}] coordinates {
            {%- for x, y in series.data %}
                ({{ x }}, {{ y }})
            {%- endfor %}
            };
        {% endfor %}

        {% for pin in pin_list %}
            \node[coordinate, pin={{ pin.location }}:{ {{ pin.text }} }]
                at (axis cs:{{ pin.x }}, {{ pin.y }}) {};
        {% endfor %}
    \end{{ '{' }}{{ axis }}}
\end{tikzpicture}
